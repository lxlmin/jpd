import Vue from 'vue'
// 在这里引入你所需的组件
import {
    Button,
    Cell,
    CellGroup,
    Icon,
    IndexBar, 
    IndexAnchor
    
} from 'vant'

// 按需引入UI组件
Vue.use(Button)
Vue.use(Cell)
Vue.use(CellGroup)
Vue.use(Icon)
Vue.use(IndexBar);
Vue.use(IndexAnchor);
